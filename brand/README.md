## Две копии спрайта

`brand/icons/sprite.svg` — исходник, `apps/web/public/brand/icons/sprite.svg` —
копия, которую отдаёт веб-сервер. Правки делаются В ИСХОДНИКЕ и копируются;
обратный порядок уже приводил к расхождению: закладка «Важное» была
перерисована только в копии, и следующее копирование из исходника откатило бы
её обратно.
