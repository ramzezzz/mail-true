# Коллективная нечёткая база rspamd.
# Штатный вес FUZZY_DENIED — 12: одной сработки хватало и на папку Спам.
# По общему правилу «одна сработка не решает» опускаем ниже порога 6;
# вместе с любым другим признаком письмо всё равно уходит в Спам.
symbols = {
    "FUZZY_DENIED" { weight = 5.5; }
    # Белая часть коллективной базы (письма, помеченные как «точно не спам»)
    "FUZZY_WHITE"  { weight = -2.0; }
}
