/**
 * Пределы сохранённого запроса, известные интерфейсу.
 *
 * Те же числа стоят в API (apps/api/src/mail/saved-searches.ts) и в схеме
 * базы (миграция 0027). Разъедутся — интерфейс разрешит набрать имя, которое
 * сервер отвергнет; поэтому число здесь одно, названо и объяснено, а не
 * рассыпано по разметке.
 */

/** Имя — подпись в левой колонке шириной 232px. */
export const MAX_SAVED_SEARCH_NAME_LENGTH = 64;
