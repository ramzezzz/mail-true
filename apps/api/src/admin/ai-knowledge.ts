/**
 * Что помощник знает об этом сервере.
 *
 * ------------------------------------------------------------------
 * ЗАЧЕМ ОТДЕЛЬНЫЙ СПРАВОЧНИК
 * ------------------------------------------------------------------
 * Разговор администратора отличается от пользовательского ровно одним:
 * от него ждут ответа не «вообще про почту», а про ЭТОТ сервер — в какой
 * раздел зайти, как называется настройка, что она сломает. Модель этого
 * знать не может: продукт свой, разделы свои, имена переменных свои.
 *
 * Поэтому справочник собирается здесь и уезжает в правила разговора.
 * Часть его — постоянный текст про устройство панели, часть — список
 * настроек, взятый из того же реестра, по которому работает раздел
 * «Настройки сервера». Второе важнее первого: реестр меняется вместе с
 * продуктом, и справочник, переписанный руками, разошёлся бы с ним через
 * месяц — а разошедшийся справочник хуже отсутствующего, потому что
 * отвечает уверенно и мимо.
 *
 * ------------------------------------------------------------------
 * ЧЕГО ЗДЕСЬ НЕТ И НЕ БУДЕТ
 * ------------------------------------------------------------------
 * ЗНАЧЕНИЙ настроек. Отдаются только имена, разделы и описания — то
 * есть устройство, а не содержимое. Пароли, ключи и секреты в реестре
 * помечены отдельно, но здесь не спасает и это: значения не берутся
 * вообще ни у одной настройки, потому что «взять все, кроме секретных» —
 * правило, которое однажды ошибётся на новой настройке, забытой
 * пометкой.
 */
import { SETTING_SECTIONS, SETTING_SPECS } from './server-settings-registry.js';

/**
 * Разделы панели: что где делается.
 *
 * Это ответ на самый частый вопрос администратора — «куда нажать». Текст
 * постоянный: разделы панели меняются несравнимо реже настроек, а
 * выводить их из маршрутизатора значило бы получить список внутренних
 * путей вместо человеческих названий.
 */
const PANEL_SECTIONS: readonly string[] = [
  'Сводка — состояние служб, очередь Postfix, место на диске, последние ошибки.',
  'Ящики — завести, заблокировать, сменить пароль, задать квоту, войти в чужой ящик по праву администратора.',
  'Алиасы — дополнительные адреса, которые складываются в существующий ящик.',
  'Домены — добавить домен, выпустить ключ DKIM, проверить записи DNS.',
  'Смена домена — переезд всей организации на другое доменное имя.',
  'Спам — пороги rspamd, списки отправителей, обучение фильтра.',
  'Наблюдение — графики почтового потока, отказы доставки, сроки сертификатов.',
  'Журналы — записи служб стека с фильтром по уровню и источнику.',
  'Аудит — кто из администраторов что сделал, и отдельно — входы в ящики.',
  'Оформление — логотип и подписи на экране входа.',
  'Настройки сервера — переменные окружения с описаниями; часть применяется сразу, часть требует перезапуска службы.',
  'Сертификат — свой сертификат или выпуск через Let’s Encrypt, автопродление.',
  'Перезапуск служб — перезапустить или пересоздать службу стека из панели.',
  'Обновления — версия продукта, образы служб, кнопки обновления кода и базовых образов.',
  'Перенос почты — перенос ящиков с чужого IMAP-сервера.',
  'Резервная копия — выгрузка и восстановление настроек.',
  'Помощник ИИ — поставщик, модель, пределы расходов, разрешённые возможности, журнал обращений.',
  'Администраторы — учётные записи панели и их роли.',
];

/** Как устроен сервер снаружи панели: службы и файлы. */
const STACK_FACTS: readonly string[] = [
  'Сервер работает в Docker Compose. Службы: postfix (приём и отправка), dovecot (хранение и IMAP), rspamd (антиспам и подпись DKIM), postgres (база), redis (кэш и сессии), unbound (свой резольвер), nginx (веб и TLS), api (сервер приложения), web (почта), admin (панель), autoconfig (автонастройка почтовых программ), clamav (антивирус), fail2ban (защита от подбора), service-agent (посредник для перезапуска служб из панели).',
  'Настройки лежат в infra/.env; панель пишет туда же — то, что задано в разделе «Настройки сервера», попадает в этот файл при пересоздании службы.',
  'Каталог продукта на сервере — обычно /opt/mailtrue. Обновление делается кнопкой в разделе «Обновления» или вручную: git pull и пересборка.',
  'Перезапуск службы из панели идёт через посредника service-agent, у которого закрытый список того, что ему разрешено. Postgres и redis в этот список не входят намеренно.',
];

/** Что делать нельзя и о чём предупреждать. */
const CAUTIONS: readonly string[] = [
  'Никогда не называй паролей, ключей и секретов: их значения тебе не передают, и придумывать их вместо ответа нельзя.',
  'Если для действия нужен доступ к серверу по ssh — так и скажи, не изображай, будто можешь его выполнить.',
  'Перед советом, который прерывает работу почты (перезапуск dovecot или postfix, обновление, смена домена), предупреди об этом одной фразой.',
  'Не выдумывай названий разделов и настроек: если нужной настройки нет в списке ниже, честно скажи, что её нет, и предложи посмотреть в разделе «Настройки сервера».',
];

/**
 * Список настроек: имя, раздел, описание.
 *
 * Без значений и без умолчаний — умолчание модель пересказала бы как
 * «сейчас стоит столько», а это неправда: значение могли изменить.
 */
function settingsCatalog(): string {
  const titles = new Map(SETTING_SECTIONS.map((section) => [section.id, section.title]));
  const lines: string[] = [];
  for (const spec of SETTING_SPECS) {
    const section = titles.get(spec.section) ?? spec.section;
    // Описание в реестре бывает многострочным — в справочнике нужна строка.
    const description = shorten(spec.description.replace(/\s+/gu, ' ').trim());
    lines.push(`${spec.key} (${section}) — ${description}`);
  }
  return lines.join('\n');
}

/**
 * Назначение настройки одной фразой.
 *
 * Справочник уходит поставщику ПРИ КАЖДОМ вопросе и оплачивается каждый
 * раз. Описания в реестре подробные — там они такими и должны быть, их
 * читает человек у поля ввода. Здесь же две сотни подробных описаний
 * превращаются в тридцать килобайт на каждую реплику разговора, то есть
 * в заметную часть дневного предела домена за один вопрос «где DKIM».
 *
 * Берём первое предложение: в нём и сказано, что настройка делает. Всё
 * остальное — оговорки и последствия, за которыми человек идёт в раздел
 * настроек, а не к помощнику.
 */
function shorten(description: string): string {
  const stop = description.search(/[.!?](\s|$)/u);
  const first = stop === -1 ? description : description.slice(0, stop + 1);
  if (first.length <= 160) return first;
  // Предложение длиннее строки таблицы — обрезаем по границе слова.
  const cut = first.slice(0, 160);
  const space = cut.lastIndexOf(' ');
  return `${space > 80 ? cut.slice(0, space) : cut}…`;
}

/**
 * Правила разговора для админского помощника.
 *
 * Возвращается ТЕКСТ, а не набор средств: ничего исполняемого модели не
 * даётся. Даже уговорив её, через разговор нельзя ни прочитать
 * настройку, ни что-либо изменить — здесь просто нечего вызвать.
 */
export function serverKnowledge(): string {
  return [
    'Ты помогаешь администратору почтового сервера Mail.True: подсказываешь, где что настраивается, что настройка изменит и как разобраться с ошибкой.',
    'У тебя НЕТ доступа к этому серверу: ты не видишь его настроек, журналов и писем и не можешь ничего на нём выполнить. Ты подсказываешь, что сделать человеку.',
    '',
    'РАЗДЕЛЫ ПАНЕЛИ:',
    ...PANEL_SECTIONS,
    '',
    'УСТРОЙСТВО СЕРВЕРА:',
    ...STACK_FACTS,
    '',
    'ПРАВИЛА:',
    ...CAUTIONS,
    '',
    'НАСТРОЙКИ СЕРВЕРА (имя, раздел, назначение). Значений здесь нет — только назначение:',
    settingsCatalog(),
  ].join('\n');
}
