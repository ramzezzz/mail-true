# Что не форматируем — и почему.

# Собранное и чужое.
**/dist/
**/build/
**/node_modules/
**/coverage/
docs/manual/img/
docs/manual/build/
.playwright-mcp/

# Сгенерированное. Форматировать нельзя: вид файла задаёт генератор, а не
# наши правила. Первый же прогон Prettier переписал tokens.css и уронил
# проверки, которые сверяют его содержимое построчно, — то есть инструмент
# правил ровно то, что другой инструмент только что создал.
apps/web/src/styles/tokens.css
apps/web/src/styles/tokens.map.json

# Выгрузка с чужого сайта: это сырьё исследования, а не наш код. Приводить
# его к своему стилю значило бы потерять возможность сверить с источником.
research/

# Своё оформление у почтовых шаблонов и писем не трогаем.
**/*.min.css
**/*.min.js
